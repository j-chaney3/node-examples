//syntax for node export
// exports.perimeter = (x, y) => 2 * (x + y);
// exports.area = (x, y) => x * y;

//syntax for es6 exports
export const perimeter = (x, y) => 2 * (x + y);
export const area = (x, y) => x * y;
